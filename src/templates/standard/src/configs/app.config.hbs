export const config = {
  port: Number(process.env.PORT) || {{port}},
  env: process.env.NODE_ENV || 'development',
  cors: {
    origins: ['http://localhost:{{port}}'],
  },
  {{#if (ne database "none")}}
  db: {
    storage: './data/{{name}}.sqlite',
  },
  {{/if}}
};
