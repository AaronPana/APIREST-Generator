import { Model, DataTypes, InferAttributes, InferCreationAttributes, CreationOptional } from 'sequelize';
import { sequelize } from '../database/connection.js';

export class {{pascalCase}} extends Model <InferAttributes<{{pascalCase}}>, InferCreationAttributes<{{pascalCase}}>> {
  // id, createdAt and updatedAt are handled automatically by Sequelize.
  // Uncomment below if you need to override id behavior:
  // declare id: CreationOptional<number>;

  // declare your fields here, for example:
  // declare name: string;
  // declare email: string;
}

{{pascalCase}}.init(
  {
    // id, createdAt and updatedAt are managed by Sequelize automatically.
    // Uncomment below if you need to override id behavior:
    // id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true },

    // define your fields here, for example:
    // name: { type: DataTypes.STRING, allowNull: false },
    // email: { type: DataTypes.STRING, allowNull: false },
  },
  {
    sequelize,
    modelName: '{{pascalCase}}',
    tableName: '{{plural}}',
    timestamps: true,
  }
);
